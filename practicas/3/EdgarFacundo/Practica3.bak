#lang racket
(define (f2 x)(* x x))


;(define (met m)(* m 1 ))

(define(cm m) (* m 100)) 

;(define(dm m) (/ ( met m) 0.1))
;(define(km m) (/ ( met m) 1000))
;
;
;
;
;(define (f2 x)(* x x))
;(define (f5 x)(*(f2 x)(f2 x) x)) 